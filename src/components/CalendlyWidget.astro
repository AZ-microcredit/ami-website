---
/**
 * CalendlyWidget: inline Calendly scheduling embed.
 *
 * Usage:
 *   <CalendlyWidget url="https://calendly.com/ACCOUNT/EVENT-TYPE" height={700} />
 *
 * The Calendly script is loaded once per page via a <script> tag; Calendly
 * itself deduplicates if multiple widgets appear on the same page.
 */
interface Props {
  /** Full Calendly embed URL, e.g. https://calendly.com/azmicrocredit/consultation */
  url: string;
  /** Widget height in px (default 700). The div's min-width is always 320px. */
  height?: number;
}

const { url, height = 700 } = Astro.props;
---

<!-- Calendly inline widget begin -->
<div
  class="calendly-inline-widget"
  data-url={url}
  style={`min-width:0;width:100%;height:${height}px;`}
></div>
<script
  type="text/javascript"
  src="https://assets.calendly.com/assets/external/widget.js"
  async
></script>
<!-- Calendly inline widget end -->

<p class="booking-link">Prefer a separate window? <a href={url} target="_blank" rel="noopener">Open the scheduling page ↗</a></p>
<style>.calendly-inline-widget{margin:32px 0}.booking-link{font:15px/1.6 Georgia,serif;text-align:center;padding:0 20px 40px}.booking-link a{color:#39732b}@media(max-width:600px){.calendly-inline-widget{height:950px!important}}</style>
